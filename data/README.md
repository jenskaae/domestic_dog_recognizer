# Data

Raw image data and generated intermediate manifests are not stored in this repository.

The project uses a manifest-based pipeline to track image paths, labels, hashes, duplicates, sources, breeds and train/validation/test splits.